{
  "name": "@muster/transport",
  "version": "0.1.0",
  "description": "Transport abstraction for Muster — WebSocket now, BLE later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
